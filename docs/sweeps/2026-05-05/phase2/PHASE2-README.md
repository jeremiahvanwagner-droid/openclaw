# REGGIE Phase 2/3 Run Book — sweep 2026-05-05

Generated by Phase 1 discovery. Option 1 (bulk vendor reclaim) selected.

## Doctrine compliance

- **P10 Mission Alignment** — sweep advances Recognize (clean state) and Restore (smaller failure surface) without altering customer-facing behavior.
- **PROTECTED bucket is sacred.** SOUL.md, .env*, .git/, .ssh/, .gnupg/, browser session caches, auth profiles — never touched.
- **Append-only audit.** REGGIE-STATE.md gets a new entry per `audit-entry-draft.md`; no prior entries are edited.

## Run order — LOCAL (`C:\Users\JeremiahVanWagner\.openclaw`)

1. `local/01-archive.ps1` — moves 287 files to `archive/2026-05-05-sweep\`. Idempotent.
2. **REVIEW** `local/INDEX.md` — confirm every move is acceptable.
3. `local/02-shred.ps1` — prompts `CONFIRM SHRED: <count> files will be permanently deleted. Reply BURN to proceed.` Then deletes the 247 individual SHRED files (LOCAL + VPS combined; LOCAL portion = whatever the script counts on this host). Logs to `archive/2026-05-05-sweep\SHRED-LOG.txt`.
4. `local/03-vendor-reclaim.ps1` — same BURN prompt. Removes vendor dir trees (Option 1). Reinstall after:
   ```powershell
   cd C:\Users\JeremiahVanWagner\.openclaw
   pnpm install            # rebuilds node_modules
   uv sync                 # or: python -m venv .venv; .\.venv\Scripts\pip install -r requirements.txt
   ```
5. Run `docker compose ps` — confirm `openclaw-bot` and `openclaw-webhook` still healthy. Vendor reclaim should not affect running containers.

> If you prefer bash flavor (WSL/git-bash), substitute `01-archive.sh`, `02-shred.sh`, `03-vendor-reclaim.sh` for the .ps1 versions.

## Run order — VPS (`root@177.7.32.224:/root/openclaw`)

1. `vps/01-archive.sh`
2. **REVIEW** `vps/INDEX.md`
3. `vps/02-shred.sh` — typed BURN gate
4. `vps/03-vendor-reclaim.sh` — typed BURN gate; then `pnpm install` in `/root/openclaw` to restore node_modules
5. `docker compose ps` and `curl -fsS http://127.0.0.1:8788/healthz` — confirm webhook and bot containers still healthy

## After both hosts complete

- Phase 4 will rebuild `REGGIE-STATE.md` from active reality (LOCAL canonical version is 9160B, 2026-05-05 08:13). The audit entry in `audit-entry-draft.md` will be appended verbatim, then promoted from `Status: PENDING` to `Status: APPLIED` once both hosts are clean.

## Files in this Phase 2 bundle

```
phase2/
├── audit-entry-draft.md          ← append to REGGIE-STATE.md when sweep completes
├── PHASE2-README.md              ← this file
├── local/
│   ├── 01-archive.ps1
│   ├── 01-archive.sh
│   ├── 02-shred-manifest.txt     ← list-before-delete (P3 audit gate)
│   ├── 02-shred.ps1
│   ├── 02-shred.sh
│   ├── 03-vendor-reclaim.ps1
│   ├── 03-vendor-reclaim.sh
│   └── INDEX.md
└── vps/
    ├── 01-archive.sh
    ├── 02-shred-manifest.txt
    ├── 02-shred.sh
    ├── 03-vendor-reclaim.sh
    └── INDEX.md
```

## Standing rules honored

- **Zero writes to disk on hosts from this sandbox.** Every action is a script you (or REGGIE) execute under the typed BURN gate.
- **When in doubt, archive over shred.** 96 UNKNOWN files were left UNKNOWN — none archived, none shredded.
- **No SOUL.md / .env / .git / auth profile / browser session touched.**
