/**
 * GHL API — social-media-posting namespace
 * Auto-generated by scripts/ghl-codegen/generate-client.mjs
 * DO NOT EDIT MANUALLY — regenerate from OpenAPI specs instead.
 *
 * 40 operations
 */

/** @typedef {import('../ghl-tenant-resolver.mjs')} TenantResolver */

/**
 * Create the social-media-posting namespace bound to a resolved tenant context.
 * @param {{ request: Function }} ctx — { request } from the client core
 * @returns {object} namespace with all social-media-posting operations
 */
export function createSocialMediaPostingNamespace(ctx) {
  const { request } = ctx;
  return {
    /**
     * Starts OAuth For Google Account
     * GET /social-media-posting/oauth/google/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     * @scopes socialplanner/oauth.readonly
     */
    async startGoogleOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/google/start', { query: _params });
    },

    /**
     * Get google business locations
     * GET /social-media-posting/oauth/{locationId}/google/locations/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getGoogleLocations(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/google/locations/${accountId}`);
    },

    /**
     * Set google business locations
     * POST /social-media-posting/oauth/{locationId}/google/locations/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async setGoogleLocations(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/google/locations/${accountId}`, { body });
    },

    /**
     * Get posts
     * POST /social-media-posting/{locationId}/posts/list
     * @param {string} locationId
     * @param {object} body — request body
     * @scopes socialplanner/post.readonly
     */
    async getPosts(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/posts/list`, { body });
    },

    /**
     * Create post
     * POST /social-media-posting/{locationId}/posts
     * @param {string} locationId
     * @param {object} body — request body
     * @scopes socialplanner/post.write
     */
    async createPost(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/posts`, { body });
    },

    /**
     * Get post
     * GET /social-media-posting/{locationId}/posts/{id}
     * @param {string} locationId
     * @param {string} id
     */
    async getPost(locationId, id) {
      return request('GET', `/social-media-posting/${locationId}/posts/${id}`);
    },

    /**
     * Edit post
     * PUT /social-media-posting/{locationId}/posts/{id}
     * @param {string} locationId
     * @param {string} id
     * @param {object} body — request body
     */
    async editPost(locationId, id, body) {
      return request('PUT', `/social-media-posting/${locationId}/posts/${id}`, { body });
    },

    /**
     * Delete Post
     * DELETE /social-media-posting/{locationId}/posts/{id}
     * @param {string} locationId
     * @param {string} id
     */
    async deletePost(locationId, id) {
      return request('DELETE', `/social-media-posting/${locationId}/posts/${id}`);
    },

    /**
     * Bulk Delete Social Planner Posts
     * POST /social-media-posting/{locationId}/posts/bulk-delete
     * @param {string} locationId
     * @param {object} body — request body
     */
    async bulkDeleteSocialPlannerPosts(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/posts/bulk-delete`, { body });
    },

    /**
     * Get Accounts
     * GET /social-media-posting/{locationId}/accounts
     * @param {string} locationId
     * @scopes socialplanner/account.readonly
     */
    async getAccount(locationId) {
      return request('GET', `/social-media-posting/${locationId}/accounts`);
    },

    /**
     * Delete Account
     * DELETE /social-media-posting/{locationId}/accounts/{id}
     * @param {string} locationId
     * @param {string} id
     * @param {object} [query]
     * @param {*} [query.companyId]
     * @param {*} [query.userId]
     */
    async deleteAccount(locationId, id, { companyId, userId } = {}) {
      const _params = {};
      if (companyId !== undefined) _params['companyId'] = companyId;
      if (userId !== undefined) _params['userId'] = userId;
      return request('DELETE', `/social-media-posting/${locationId}/accounts/${id}`, { query: _params });
    },

    /**
     * Starts OAuth For Facebook Account
     * GET /social-media-posting/oauth/facebook/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     */
    async startFacebookOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/facebook/start', { query: _params });
    },

    /**
     * Get facebook pages
     * GET /social-media-posting/oauth/{locationId}/facebook/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getFacebookPageGroup(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/facebook/accounts/${accountId}`);
    },

    /**
     * Attach facebook pages
     * POST /social-media-posting/oauth/{locationId}/facebook/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async attachFacebookPageGroup(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/facebook/accounts/${accountId}`, { body });
    },

    /**
     * Starts OAuth For Instagram Account
     * GET /social-media-posting/oauth/instagram/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     * @scopes socialplanner/oauth.readonly
     */
    async startInstagramOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/instagram/start', { query: _params });
    },

    /**
     * Get Instagram Professional Accounts
     * GET /social-media-posting/oauth/{locationId}/instagram/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getInstagramPageGroup(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/instagram/accounts/${accountId}`);
    },

    /**
     * Attach Instagram Professional Accounts
     * POST /social-media-posting/oauth/{locationId}/instagram/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async attachInstagramPageGroup(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/instagram/accounts/${accountId}`, { body });
    },

    /**
     * Starts OAuth For LinkedIn Account
     * GET /social-media-posting/oauth/linkedin/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     */
    async startLinkedinOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/linkedin/start', { query: _params });
    },

    /**
     * Get Linkedin pages and profile
     * GET /social-media-posting/oauth/{locationId}/linkedin/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getLinkedinPageProfile(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/linkedin/accounts/${accountId}`);
    },

    /**
     * Attach linkedin pages and profile
     * POST /social-media-posting/oauth/{locationId}/linkedin/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async attachLinkedinPageProfile(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/linkedin/accounts/${accountId}`, { body });
    },

    /**
     * Starts OAuth For Twitter Account
     * GET /social-media-posting/oauth/twitter/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     * @scopes socialplanner/oauth.readonly
     */
    async startTwitterOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/twitter/start', { query: _params });
    },

    /**
     * Get Twitter profile
     * GET /social-media-posting/oauth/{locationId}/twitter/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getTwitterProfile(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/twitter/accounts/${accountId}`);
    },

    /**
     * Attach Twitter profile
     * POST /social-media-posting/oauth/{locationId}/twitter/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async attachTwitterProfile(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/twitter/accounts/${accountId}`, { body });
    },

    /**
     * Get Upload Status
     * GET /social-media-posting/{locationId}/csv
     * @param {string} locationId
     * @param {object} [query]
     * @param {*} [query.skip]
     * @param {*} [query.limit]
     * @param {*} [query.includeUsers]
     * @param {*} [query.userId]
     */
    async getUploadStatus(locationId, { skip, limit, includeUsers, userId } = {}) {
      const _params = {};
      if (skip !== undefined) _params['skip'] = skip;
      if (limit !== undefined) _params['limit'] = limit;
      if (includeUsers !== undefined) _params['includeUsers'] = includeUsers;
      if (userId !== undefined) _params['userId'] = userId;
      return request('GET', `/social-media-posting/${locationId}/csv`, { query: _params });
    },

    /**
     * Upload CSV
     * POST /social-media-posting/{locationId}/csv
     * @param {string} locationId
     * @param {object} body — request body
     * @scopes socialplanner/csv.write
     */
    async uploadCsv(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/csv`, { body, isMultipart: true });
    },

    /**
     * Set Accounts
     * POST /social-media-posting/{locationId}/set-accounts
     * @param {string} locationId
     * @param {object} body — request body
     * @scopes socialplanner/csv.write
     */
    async setAccounts(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/set-accounts`, { body });
    },

    /**
     * Get CSV Post
     * GET /social-media-posting/{locationId}/csv/{id}
     * @param {string} locationId
     * @param {string} id
     * @param {object} [query]
     * @param {*} [query.skip]
     * @param {*} [query.limit]
     */
    async getCsvPost(locationId, id, { skip, limit } = {}) {
      const _params = {};
      if (skip !== undefined) _params['skip'] = skip;
      if (limit !== undefined) _params['limit'] = limit;
      return request('GET', `/social-media-posting/${locationId}/csv/${id}`, { query: _params });
    },

    /**
     * Start CSV Finalize
     * PATCH /social-media-posting/{locationId}/csv/{id}
     * @param {string} locationId
     * @param {string} id
     * @param {object} body — request body
     */
    async startCsvFinalize(locationId, id, body) {
      return request('PATCH', `/social-media-posting/${locationId}/csv/${id}`, { body });
    },

    /**
     * Delete CSV
     * DELETE /social-media-posting/{locationId}/csv/{id}
     * @param {string} locationId
     * @param {string} id
     */
    async deleteCsv(locationId, id) {
      return request('DELETE', `/social-media-posting/${locationId}/csv/${id}`);
    },

    /**
     * Delete CSV Post
     * DELETE /social-media-posting/{locationId}/csv/{csvId}/post/{postId}
     * @param {string} locationId
     * @param {string} postId
     * @param {string} csvId
     */
    async deleteCsvPost(locationId, postId, csvId) {
      return request('DELETE', `/social-media-posting/${locationId}/csv/${csvId}/post/${postId}`);
    },

    /**
     * Starts OAuth For Tiktok Account
     * GET /social-media-posting/oauth/tiktok/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     * @scopes socialplanner/oauth.readonly
     */
    async startTiktokOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/tiktok/start', { query: _params });
    },

    /**
     * Get Tiktok profile
     * GET /social-media-posting/oauth/{locationId}/tiktok/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getTiktokProfile(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/tiktok/accounts/${accountId}`);
    },

    /**
     * Attach Tiktok profile
     * POST /social-media-posting/oauth/{locationId}/tiktok/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     * @param {object} body — request body
     */
    async attachTiktokProfile(locationId, accountId, body) {
      return request('POST', `/social-media-posting/oauth/${locationId}/tiktok/accounts/${accountId}`, { body });
    },

    /**
     * Starts OAuth For Tiktok Business Account
     * GET /social-media-posting/oauth/tiktok-business/start
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {*} [query.userId] (required)
     * @param {*} [query.page]
     * @param {*} [query.reconnect]
     * @scopes socialplanner/oauth.readonly
     */
    async startTiktokBusinessOauth({ locationId, userId, page, reconnect } = {}) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      if (userId !== undefined) _params['userId'] = userId;
      if (page !== undefined) _params['page'] = page;
      if (reconnect !== undefined) _params['reconnect'] = reconnect;
      return request('GET', '/social-media-posting/oauth/tiktok-business/start', { query: _params });
    },

    /**
     * Get Tiktok Business profile
     * GET /social-media-posting/oauth/{locationId}/tiktok-business/accounts/{accountId}
     * @param {string} locationId
     * @param {string} accountId
     */
    async getTiktokBusinessProfile(locationId, accountId) {
      return request('GET', `/social-media-posting/oauth/${locationId}/tiktok-business/accounts/${accountId}`);
    },

    /**
     * Get categories by location id
     * GET /social-media-posting/{locationId}/categories
     * @param {string} locationId
     * @param {object} [query]
     * @param {*} [query.searchText]
     * @param {*} [query.limit]
     * @param {*} [query.skip]
     * @scopes socialplanner/category.readonly
     */
    async getCategoriesLocationId(locationId, { searchText, limit, skip } = {}) {
      const _params = {};
      if (searchText !== undefined) _params['searchText'] = searchText;
      if (limit !== undefined) _params['limit'] = limit;
      if (skip !== undefined) _params['skip'] = skip;
      return request('GET', `/social-media-posting/${locationId}/categories`, { query: _params });
    },

    /**
     * Get categories by id
     * GET /social-media-posting/{locationId}/categories/{id}
     * @param {string} id
     * @param {string} locationId
     */
    async getCategoriesId(id, locationId) {
      return request('GET', `/social-media-posting/${locationId}/categories/${id}`);
    },

    /**
     * Get tags by location id
     * GET /social-media-posting/{locationId}/tags
     * @param {string} locationId
     * @param {object} [query]
     * @param {*} [query.searchText]
     * @param {*} [query.limit]
     * @param {*} [query.skip]
     */
    async getTagsLocationId(locationId, { searchText, limit, skip } = {}) {
      const _params = {};
      if (searchText !== undefined) _params['searchText'] = searchText;
      if (limit !== undefined) _params['limit'] = limit;
      if (skip !== undefined) _params['skip'] = skip;
      return request('GET', `/social-media-posting/${locationId}/tags`, { query: _params });
    },

    /**
     * Get tags by ids
     * POST /social-media-posting/{locationId}/tags/details
     * @param {string} locationId
     * @param {object} body — request body
     */
    async getTagsByIds(locationId, body) {
      return request('POST', `/social-media-posting/${locationId}/tags/details`, { body });
    },

    /**
     * Get Social Media Statistics
     * POST /social-media-posting/statistics
     * @param {object} [query]
     * @param {*} [query.locationId] (required)
     * @param {object} body — request body
     */
    async getSocialMediaStatistics({ locationId } = {}, body) {
      const _params = {};
      if (locationId !== undefined) _params['locationId'] = locationId;
      return request('POST', '/social-media-posting/statistics', { query: _params, body });
    }
  };
}

/** Scope requirements for each operation in this namespace. */
export const SCOPES = {
  startGoogleOauth: ["socialplanner/oauth.readonly"],
  getPosts: ["socialplanner/post.readonly"],
  createPost: ["socialplanner/post.write"],
  getAccount: ["socialplanner/account.readonly"],
  startInstagramOauth: ["socialplanner/oauth.readonly"],
  startTwitterOauth: ["socialplanner/oauth.readonly"],
  uploadCsv: ["socialplanner/csv.write"],
  setAccounts: ["socialplanner/csv.write"],
  startTiktokOauth: ["socialplanner/oauth.readonly"],
  startTiktokBusinessOauth: ["socialplanner/oauth.readonly"],
  getCategoriesLocationId: ["socialplanner/category.readonly"],
};
